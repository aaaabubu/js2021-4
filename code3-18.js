const repl = require('relp');

repl.start({
  prompt: "숫자를 입력하세요 >",
  eval: (command, context, filename, callback) => {
    let (isNaN(number)) {

    } else {
      console.log("숫자입니다.");
    }
  } callback();
}) 